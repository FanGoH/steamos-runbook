# Re-apply ~/.config/pad-hide/hidden.json after a joystick appears (replug).
# Install: sudo install -m 644 udev/99-hide-controllers.rules /etc/udev/rules.d/
ACTION=="add", SUBSYSTEM=="input", KERNEL=="js*", RUN+="/usr/bin/python3 /home/deck/steamos-playbook/scripts/hide-controllers.py apply"
